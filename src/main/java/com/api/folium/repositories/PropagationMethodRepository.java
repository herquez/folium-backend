package com.api.folium.repositories;

import com.api.folium.entities.PropagationMethod;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PropagationMethodRepository extends JpaRepository<PropagationMethod, Long> {
}
